- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.config/alacritty/alacritty.yml: alacritty.yml
    ~/.config/nvim/lua/config.lua: config.lua
    ~/.tmux.conf: tmux.conf
    ~/.tmuxinator/avarni.yml: tmuxinator/avarni.yml

- shell:
  - [git submodule update --init --recursive, Installing submodules]
